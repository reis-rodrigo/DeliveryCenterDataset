IMPORT $;

// OUTPUT($.record_raw.hubs);
// OUTPUT($.record_raw.payments);
// OUTPUT($.record_raw.stores);
// OUTPUT($.record_raw.deliveries);
// OUTPUT($.record_raw.drivers);
// OUTPUT($.record_raw.channels);
// OUTPUT($.record_raw.orders);

OUTPUT($.record_optimized.hubs);
OUTPUT($.record_optimized.payments);
OUTPUT($.record_optimized.stores);
OUTPUT($.record_optimized.deliveries);
OUTPUT($.record_optimized.drivers);
OUTPUT($.record_optimized.channels);
OUTPUT($.record_optimized.orders);

